spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:tc:postgresql://localhost:5438/helpdeskdb-test
    username: helpdesk-test-user
    password: 'test'

  jpa:
    hibernate:
      ddl-auto: create-drop