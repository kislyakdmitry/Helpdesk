version: '3.3'

services:

  helpdesk_test:
    image: 'postgres:13.2'

    ports:
      - 5438:5432
    networks:
      - app_net

    environment:
      POSTGRES_USER: helpdesk-user-test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: helpdesk-test

networks:
  app_net:
    driver: bridge