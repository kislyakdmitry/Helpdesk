<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">

    <changeSet id="1" author="Alexey Zuevsky">
        <comment>1.1.version. Changing table Ticket(state_id): type ENUM -> varchar</comment>
        <sql>
            ALTER TABLE Ticket
                ALTER COLUMN state_id SET DATA TYPE varchar( 40 );
        </sql>
    </changeSet>

    <changeSet id="2" author="Alexey Zuevsky">
        <comment>1.1.version. Creating constraint for Ticket(state_id): CHECK </comment>
        <sql>
            ALTER TABLE Ticket
            ADD CONSTRAINT state_id_enum CHECK ( state_id IN ( 'DRAFT', 'NEW', 'APPROVED', 'DECLINED', 'IN_PROGRESS', 'DONE', 'CANCELED' ) );
        </sql>
    </changeSet>

    <changeSet id="3" author="Alexey Zuevsky">
        <comment>1.1.version. Changing table Ticket(urgency_id): type ENUM -> varchar</comment>
        <sql>
            ALTER TABLE Ticket
                ALTER COLUMN urgency_id SET DATA TYPE varchar( 40 );
        </sql>
    </changeSet>

    <changeSet id="4" author="Alexey Zuevsky">
        <comment>1.1.version. Creating constraint for Ticket(urgency_id): CHECK </comment>
        <sql>
            ALTER TABLE Ticket
            ADD CONSTRAINT urgency_id_enum CHECK ( urgency_id IN ( 'CRITICAL', 'HIGH', 'AVERAGE', 'LOW' ) );
        </sql>
    </changeSet>

    <changeSet id="5" author="Alexey Zuevsky">
        <comment>1.1.version. Changing table UserCard(role_id): type ENUM -> varchar</comment>
        <sql>
            ALTER TABLE UserCard
                ALTER COLUMN role_id SET DATA TYPE varchar( 40 );
        </sql>
    </changeSet>

    <changeSet id="6" author="Alexey Zuevsky">
        <comment>1.1.version. Creating constraint for UserCard(role_id): CHECK </comment>
        <sql>
            ALTER TABLE UserCard
            ADD CONSTRAINT role_id_enum CHECK ( role_id IN ( 'EMPLOYEE', 'MANAGER', 'ENGINEER' ) );
        </sql>
    </changeSet>

    <changeSet id="7" author="Alexey Zuevsky">
        <comment>1.1.version. Drop ENUM ticket_state_id</comment>
        <sql>
            DROP TYPE ticket_state_id;
        </sql>
    </changeSet>

    <changeSet id="8" author="Alexey Zuevsky">
        <comment>1.1.version. Drop ENUM ticket_urgency_id</comment>
        <sql>
            DROP TYPE ticket_urgency_id;
        </sql>
    </changeSet>

    <changeSet id="9" author="Alexey Zuevsky">
        <comment>1.1.version. Drop ENUM user_role_id</comment>
        <sql>
            DROP TYPE user_role_id;
        </sql>
    </changeSet>

    <changeSet id="10" author="Alexey Zuevsky">
        <comment>1.1.version. Changing table Ticket(created_on): type date -> timestamp</comment>
        <sql>
            ALTER TABLE Ticket
                ALTER COLUMN created_on SET DATA TYPE timestamp;
        </sql>
    </changeSet>

    <changeSet id="11" author="Alexey Zuevsky">
        <comment>1.1.version. Changing table Attachment(blob): name -> link</comment>
        <sql>
            ALTER TABLE Attachment
                RENAME COLUMN blob TO link;
        </sql>
    </changeSet>

</databaseChangeLog>